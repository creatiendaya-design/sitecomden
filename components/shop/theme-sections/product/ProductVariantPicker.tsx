"use client"

import type { ResolvedThemeSectionBlock } from "@/lib/theme-sections/types"

interface ProductVariantPickerProps {
  block: ResolvedThemeSectionBlock
}

// Stub — full implementation lands in Task C4 (stateful client sub-blocks).
export function ProductVariantPicker(_props: ProductVariantPickerProps) {
  return null
}
